<template>
  <div class="container">
    <text class="icon">🎉</text>
    <text class="title">恭喜通关！</text>
    <text class="message">成功将所有箱子推到目标点！</text>
    
    <div class="buttons">
      <div class="button next-btn" onclick="nextLevel">
        <text class="button-text">下一关</text>
      </div>
      <div class="button restart-btn" onclick="restartLevel">
        <text class="button-text">重玩</text>
      </div>
    </div>
  </div>
</template>

<script>
import router from '@system.router' 
// 当一关游戏结束后，跳转到这个页面。这个页面有三个按钮：下一关、重玩和返回主菜单。
// 点击下一关会跳转到下一关的页面，点击重玩会重新加载当前关卡。
// 返回主菜单暂时没有，后续可以添加一个按钮实现这个功能。

export default {
  private: {
    levelId: '',
    levelPath: '',
  },

  onInit() {
    // 获取当前关卡信息
    const { levelId, levelPath } = this.$page.options
    this.levelId = levelId || 'level01'
    this.levelPath = levelPath || 'pages/levels/level01/index.ux'
  },

  onBackPress() {
    // 拦截返回键
    this.restartLevel()
    return true
  },
  
  nextLevel() {
    // 跳转到下一关
    router.replace({
      uri: `/${this.levelPath}`
    })
  },
  
  restartLevel() {
    router.replace({
      uri: `/${this.levelPath}`
    })
  }

  
}
</script>

<style scoped>
.container {
  flex-direction: column;
  align-items: center;
  justify-content: center;
  background-color: #1a1a1a;
  height: 100%;
  width: 100%;
  padding: 20px;
}

.icon {
  font-size: 60px;
  margin-bottom: 20px;
  lines: 1;
}

.title {
  font-size: 18px;
  color: #ffffff;
  font-weight: bold;
  margin-bottom: 12px;
}

.message {
  font-size: 14px;
  color: #cccccc;
  margin-bottom: 30px;
  text-align: center;
  lines: 2;
}

.buttons {
  flex-direction: row;
}

.button {
  min-width: 70px;
  height: 40px;
  border-radius: 8px;
  align-items: center;
  justify-content: center;
  padding: 0 16px;
  margin: 0 8px;
}

.button-text {
  font-size: 16px;
  color: #ffffff;
}

.next-btn {
  background-color: #667eea;
}

.restart-btn {
  background-color: #ff416c;
}
</style>
